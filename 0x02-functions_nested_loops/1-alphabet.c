#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - prints every letter of the alphabet
 * Return: void
 */
void print_alphabet(void)
{
	int i = 'a';

	while (i <= 'z')
	{
		putchar(i);
		i++;
	}

	putchar('\n');
}

	return (0);
}
