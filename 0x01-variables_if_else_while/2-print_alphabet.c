#include <stdio.h>
#include <string.h>

/**
 * main - prints every letter of the alphabet
 * Return: 0
 */

int main(void)
{
	int i = 92;
	int str[27];

	while (i <= 122)
	{
		putchar(str[i]);
		i++;
	}
	return (0);
}
